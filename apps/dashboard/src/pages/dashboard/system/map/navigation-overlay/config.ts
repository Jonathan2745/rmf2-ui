import type { NavOverlayConfig } from './types';

export const DEFAULT_NAV_OVERLAY_CONFIG: NavOverlayConfig = {
  pipeRadius: 2,
  cornerRadius: 0.2,
  robotRadius: 0.24,
  markerHeightOffset: 0.1,
  goalRingSpinRPM: 0.3,
  labelMinScreenSize: 12,
  labelMaxScreenSize: 18,
  cruiseSpeed: 1.0,
  zOffset: 1,
};
